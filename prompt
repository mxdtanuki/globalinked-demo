Fix all ESLint errors causing production build failure (Vercel CI treats warnings as errors).

STRICT RULES
Do NOT refactor architecture or redesign components.
Do NOT remove features or UI logic.
Only apply minimal fixes required to satisfy ESLint.
Do NOT add new comments.
Keep existing indentation and formatting unless required for fix.
CRITICAL BUILD-STOPPING ISSUES TO FIX
1. import/no-anonymous-default-export

File: src/adminDummyData.js

Export is currently anonymous object.
Fix by assigning the object to a named constant first, then exporting it.
2. React Hook dependency issues (react-hooks/exhaustive-deps)

Fix missing dependencies in useEffect hooks:

src/components/moaActChart.jsx → add chartData
src/components/moaChart.jsx → add chartData
src/components/mouChart.jsx → add chartData
src/pages/analytics.jsx → fix missing deps (fetchAndProcessData, MONTHS_ORDER, getMonthsForYear, getYearlyData)
src/pages/email.jsx → add selectedAgreement
src/pages/userManagement.jsx → add fetchAllUsers

If a dependency causes re-render loops:

Wrap functions or objects in useCallback or useMemo instead of removing dependency.
3. Unused variables (no-unused-vars)

Remove or properly use:

overviewDash.jsx → remove unused imports:
FiChevronDown, FiGrid, FiActivity
QueryClient, QueryClientProvider
ExcelJS, saveAs
STATUS_THRESHOLDS, regionOptions, etc.
topbarSidebar.jsx
remove isDesktop
activeAgreement.jsx
remove unused setters and variables (setReportType, editingField, etc.)
docVer.jsx
remove unused icons and currentUser
mobility.jsx
remove jsPDF, autoTable, ExcelJS, saveAs if unused
resetPass.jsx
remove unused token
upload/moa.jsx
remove unused icons
archive.jsx
remove FiGrid and escapeHtml if unused
upload/extractedEntryMoa.jsx
remove dtsStatus, autoFilledFields if unused
4. Unsafe equality

File: src/services/agreementService.js

Replace all == with ===
5. Other ESLint rules
Fix no-useless-escape in regex (remove unnecessary backslashes)
Ensure stable references for agreements object in overviewDash.jsx
Wrap in useMemo to prevent dependency churn
IMPORTANT BUILD RULE

After fixes:

Run npm run build
Ensure ZERO ESLint errors
Do not suppress lint rules globally unless absolutely necessary
GOAL

Make Vercel production build succeed without disabling ESLint or CI checks.